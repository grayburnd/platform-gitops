apiVersion: v1
kind: ConfigMap
metadata:
  name: fargate-observability-config
  namespace: "aws-observability"
data:
  output.conf: |
    [OUTPUT]
        Name cloudwatch_logs
        Match   *
        region {{ .Values.aws.region }}
        log_group_name fluent-bit-cloudwatch
        log_stream_prefix from-fluent-bit-
        auto_create_group true