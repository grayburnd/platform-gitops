apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.grafanaExternalSecret.name }}
  namespace: {{ .Values.team }}-{{ .Values.environment }}
spec:
  refreshPolicy: CreatedOnce
  target:
    name: {{ .Values.grafanaExternalSecret.name }}
    # keep the Secret if this grafanaExternalSecret is ever deleted
    creationPolicy: Orphan
    # never rewrite the Secret data once it exists, even on recreation
    immutable: false
  dataFrom:
    # This grabs EVERY secret key value pair under the specified secret.
    - extract:
        key: {{ .Values.grafanaExternalSecret.name }}
      sourceRef:
        storeRef:
          name: {{ .Values.grafanaExternalSecret.storeRef }}
          kind: ClusterSecretStore